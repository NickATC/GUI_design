import tkinter as tk
from tkinter import ttk

window = tk.Tk()
window.title("LogIn")
window.geometry('280x150')
window.resizable(0,0)
window.wm_iconbitmap('images/human.ico')

#Parent window name is window
#start coding the widgets:


window.mainloop()
